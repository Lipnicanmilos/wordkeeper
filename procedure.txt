# Vytvorenie env
python -m venv venv

# Pre Windows PowerShell:
.\venv\Scripts\Activate.ps1

# Alebo pre Command Prompt:
.\venv\Scripts\activate.bat

# Najprv aktualizujte pip
python -m pip install --upgrade pip

# Potom nainštalujte balíčky
pip install fastapi uvicorn sqlalchemy pydantic

# Tento príkaz zobrazí nainštalované balíčky
pip list

# Alebo skontrolujte cestu k Pythonu
where python
Get-Command python

python --version


uvicorn app.main:app --reload


#Zistite nainštalované balíčky:
pip freeze

#Uložte ich do requirements.txt:
pip freeze > requirements.txt


user01@user.com
user02@user.com
user03@user.com
User1111

-----------------------------------------------------

git add .
git commit -m "Add new feature: user authentication"
git push

Natvrdo resetni lokálnu vetvu na požadovaný commit: Toto zahodí všetky lokálne zmeny a odstráni z lokálnej histórie commity, ktoré nasledovali po 967572f.
Bash
git reset --hard 967572f59d5c580f29e0fdc2b411d926cc01281b


Súhrn pre FastAPI/Uvicorn:

Pole	Odporúčané nastavenie
Build context directory	/
Entrypoint	uvicorn app.main:app --host 0.0.0.0 --port $PORT
Function target	Prázdne

google autentifikacia
https://console.cloud.google.com/auth/clients?project=grand-object-333523

google console
https://console.cloud.google.com/run/detail/europe-west3/wordkeeper/observability/metrics?chat=true&project=grand-object-333523

my text
--comment
----------------------------------------------
Chcete pridať tlačidlo „Opakovať slovicka ktore neviem“ 
„Opakovať slovicka ktore sa ucitm “ a 
„Opakovať slovicka ktore viem“vedľa existujúcich testovacích tlačidiel v riadiacom paneli pre každú úroveň kategórie? 
Funkcia opakovania bude automaticky prechádzať náhodne vybranými slovami, zobrazovať originál po dobu 2 sekúnd, potom preklad po dobu 3 sekúnd a tak ďalej až do konca ale pritom kazde jedno bude aj audio sprevadzane