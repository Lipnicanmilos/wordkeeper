[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra -q
filterwarnings =
    # Cudzia knižnica fastapi_mail (nedá sa opraviť z nášho kódu) — stíš len ju.
    ignore::DeprecationWarning:fastapi_mail.schemas
